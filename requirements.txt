# 基础依赖（必需）
fastapi>=0.111.1
uvicorn[standard]
python-multipart
websockets
python-dotenv

# 数据处理
numpy<=2.3.3

# 机器学习框架
# 注意：请根据你的环境选择合适的PyTorch版本

# CPU版本（默认安装）
torch>=2.0.0
torchaudio>=2.0.0

# GPU显存管理（可选，用于准确检测GPU显存使用情况）
pynvml

# GPU版本安装说明：
# 在安装requirements.txt之前，请先单独安装GPU版本的PyTorch：
# 
# CUDA 11.8:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# CUDA 12.1:  
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# 然后再运行: pip install -r requirements.txt

# 音频处理
pydub>=0.25.1

# SenseVoice相关
modelscope
huggingface_hub
funasr>=1.1.3

# 可选依赖
gradio
